@model IEnumerable<CarPartsAccounting.Models.workers>

@{
    ViewBag.Title = "Работники";
}

<h2>Работники</h2>

<a type="button" style="margin: 0px 0px 20px;" href="@Url.Action("Create", "workers")" class="btn btn-info">Добавить</a>

<table class="table table-striped">
    <tr>
        <th>
            Имя работника
        </th>
        <th>
            Должность
        </th>
        <th>
            Номер телефона
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.worker_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.phone_number)
            </td>
            <td>
                <a type="button" href="@Url.Action("Edit", new { id = item.id })" class="btn btn-info">
                    <span>Редактировать</span>
                </a>
                <a type="button" href="@Url.Action("Delete", new { id = item.id })" class="btn btn-danger">
                    <span>Удалить</span>
                </a>
            </td>
        </tr>
    }

</table>
